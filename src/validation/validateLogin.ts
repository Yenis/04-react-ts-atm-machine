// import { allUserKeys } from "../data/allAccountIDs"

export const isNotRegistered = (cardNumber: string) => {
    // allAccounts.forEach(account => {
    //     if (cardNumber === account) {
    //        return true;
    //     } else {
    //         alert(`User Account ${cardNumber} Doesn't Exist`)
    //         return false;
    //     }
    // })
    return true;
}

export const isPinCorrect = (pinNumber: string) => {
    return true;
}
